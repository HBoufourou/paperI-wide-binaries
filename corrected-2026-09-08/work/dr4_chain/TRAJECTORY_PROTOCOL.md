# Coupled physical checks before production libraries

8 September 2026. Use InteractionModel.load with its final-validation requirement enabled. Never use an unvalidated interpolant in the production generator. Preserve the model, its source hashes, force/energy checks, the independent energy report and this protocol in the evidence chain.

The trajectory integrator is kick–drift–kick using the analytic gradient of the same scalar potential as the energy sampler. The first state is at t=0, before the observation epochs. Source barycentric fractions are actual source masses, including any hidden inner mass. The model's global Cmax over both gravities/three mass ratios is used as a common conservative invariant restriction for every library. A numerical bound for the interpolation is not a bound on the real astrophysical population.

For each gravity, generate512 validation-only companion40 states using seeds268000101 and268001101, never used for fitting/calibration/test. At all observing epochs in each of the3 scenarios, propagate with one and two substeps per epoch interval. Require maximum relative energy change<10^-7, absolute Lz change<10^-10, displacement difference between refinements<10^-5 AU, and no out-of-domain evaluation. Fit outer+inner+noise jointly using observer seeds268000201/268001201 and require measured proper motion to equal the saved deterministic/noise decomposition within10^-10 mas/year. The full coupled diagnostics must be finite, and the full shared-covariance identity must hold to10^-9 relative. Parent selection and initial photocentre offsets are checked independently from the saved states.

For the first24 validation states satisfying rlo>0.2 and initial r<4, run longer diagnostic trajectories over dimensionless time2 with2000 and4000 uniform steps, using an artificial common mass of1 solar mass only to convert time units. Require relative energy error of the finer run<5×10^-4, maximum absolute Lz change<10^-9 and an energy-refinement ratio<0.4 whenever coarse error exceeds10^-9. These trajectories test numerical conservation for a bounded subset; they are not a statement that selected catalogues retain their marginal distributions after time evolution. The distribution parent is stationary by its dependence on E,Lz; the observational selection is instantaneous.

Save initial states, short trajectory positions/velocities, complete joint-observer outputs and coarse/fine long trajectories. No failed check is discarded. Production requires every declared check to pass, plus the upstream final physical validation. This authorizes only the stated conditional population experiment, not a Newton/MOND verdict on actual Gaia observations. An independent reconstruction will check the completed scientific libraries and decision results separately.
